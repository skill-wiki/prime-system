{
  "name": "skill-wiki",
  "version": "0.2.0",
  "description": "Skill Wiki — typed atoms, edge graph, lazy projection. Like Wikipedia, but for AI agents.",
  "license": "Apache-2.0",
  "homepage": "https://github.com/skill-wiki/skill-wiki",
  "repository": {
    "type": "git",
    "url": "https://github.com/skill-wiki/skill-wiki.git"
  },
  "bugs": {
    "url": "https://github.com/skill-wiki/skill-wiki/issues"
  },
  "keywords": [
    "skill-wiki",
    "prime",
    "knowledge-protocol",
    "ai-agents",
    "mcp",
    "claude",
    "atoms",
    "knowledge-graph"
  ],
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "compile-examples": "bun scripts/build-atom-dirs.ts --src examples/hello-world/primes/sources --out examples/hello-world/primes/compiled --release 2026-08-29 && bun scripts/build-atom-dirs.ts --src examples/recipes/primes/sources --out examples/recipes/primes/compiled --release 2026-08-29 && bun scripts/build-atom-dirs.ts --src examples/coding-style/primes/sources --out examples/coding-style/primes/compiled --release 2026-08-29",
    "registry-server": "bun scripts/registry-server.ts",
    "test-roundtrip": "bash scripts/test-registry-roundtrip.sh"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "bun-types": "^1.3.12",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "yaml": "^2.8.3",
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
