{
  "name": "@aoe/plugin-host",
  "version": "0.2.0",
  "description": "Plugin discovery, capability authorization and process-isolated execution (plan §12): deny-by-default, no ambient host handles",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "sandbox-child": "bun src/executor/child.ts"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "yaml": "^2.8.3",
    "zod": "^4.0.0"
  }
}
