{
  "name": "@skill-wiki/policy-engine",
  "version": "0.1.0",
  "description": "Declaration-driven policy engine: principal, capability, side-effect and approval decisions read from a declared policy set",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "test": "bun test", "typecheck": "tsc --noEmit -p tsconfig.json" },
  "license": "Apache-2.0",
  "dependencies": { "@skill-wiki/model-schema": "workspace:*", "yaml": "^2.8.3", "zod": "^4.0.0" }
}
