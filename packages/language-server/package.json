{
  "name": "@skill-wiki/language-server",
  "version": "0.2.0",
  "description": "Kernary Language Server for editor diagnostics and completion; it never builds a runtime snapshot",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@skill-wiki/types": "workspace:*",
    "@skill-wiki/parser": "workspace:*",
    "@skill-wiki/model-schema": "workspace:*",
    "@skill-wiki/compiler": "workspace:*"
  }
}
