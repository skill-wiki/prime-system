{
  "name": "@aoe/language-server",
  "version": "0.2.0",
  "description": "AOE Language Server for editor diagnostics and completion; it never builds a runtime snapshot",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@aoe/types": "workspace:*",
    "@aoe/parser": "workspace:*",
    "@aoe/model-schema": "workspace:*",
    "@aoe/compiler": "workspace:*"
  }
}
