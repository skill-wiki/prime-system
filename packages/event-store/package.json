{
  "name": "@aoe/event-store",
  "version": "0.2.0",
  "description": "Append-only, multi-tenant event and run-record store with replay and persistent idempotency",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": { "test": "bun test", "typecheck": "tsc --noEmit -p tsconfig.json" },
  "license": "Apache-2.0"
}
