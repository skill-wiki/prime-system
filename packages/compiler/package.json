{
  "name": "@aoe/compiler",
  "version": "0.2.0",
  "description": "AOE compiler for .prime source files — structural, logic, and domain checking with optimized Markdown emission",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aoe/types": "workspace:*",
    "@aoe/parser": "workspace:*",
    "@aoe/model-schema": "workspace:*",
    "@aoe/ir": "workspace:*"
  },
  "license": "Apache-2.0"
}
