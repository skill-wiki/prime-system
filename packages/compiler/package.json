{
  "name": "@prime-lang/compiler",
  "version": "0.1.0",
  "description": "AI-powered compiler for .prime files — structural, logic, and domain checking with optimized Markdown emission",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prime-lang/types": "workspace:*",
    "@prime-lang/parser": "workspace:*"
  },
  "license": "Apache-2.0"
}
