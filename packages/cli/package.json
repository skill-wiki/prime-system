{
  "name": "@skill-wiki/cli",
  "version": "0.2.0",
  "description": "AOE CLI — author, compile, inspect, query, and operate Agent Ontology Packages.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "aoe": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun && node -e \"const f='dist/index.js';const fs=require('fs');let s=fs.readFileSync(f,'utf8');if(s.startsWith('#!'))s=s.replace(/^#![^\\n]*\\n/,'#!/usr/bin/env bun\\n');else s='#!/usr/bin/env bun\\n'+s;fs.writeFileSync(f,s);fs.chmodSync(f,0o755);\" && bun dist/index.js --version",
    "prepublishOnly": "bun run build",
    "test": "bun test --pass-with-no-tests",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@skill-wiki/action-runtime": "workspace:*",
    "@skill-wiki/bundle": "workspace:*",
    "@skill-wiki/compiler": "workspace:*",
    "@skill-wiki/event-store": "workspace:*",
    "@skill-wiki/ir": "workspace:*",
    "@skill-wiki/language-server": "workspace:*",
    "@skill-wiki/model-schema": "workspace:*",
    "@skill-wiki/parser": "workspace:*",
    "@skill-wiki/runtime": "workspace:*",
    "@skill-wiki/types": "workspace:*"
  },
  "keywords": [
    "aoe",
    "knowledge-runtime",
    "mcp",
    "ai-agents",
    "cli"
  ],
  "engines": {
    "bun": ">=1.3.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skill-wiki/kernary-engine.git",
    "directory": "packages/cli"
  },
  "homepage": "https://github.com/skill-wiki/kernary-engine/tree/main/packages/cli#readme",
  "bugs": {
    "url": "https://github.com/skill-wiki/kernary-engine/issues"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "license": "Apache-2.0"
}
