{
  "name": "@aoe/http-server",
  "version": "0.2.0",
  "description": "Authenticated HTTP transport over the kernel's snapshot/plan/query/resource surface. Loopback by default, bearer-credentialed, OTLP-traced.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "aoe-http-server": "src/main.ts"
  },
  "scripts": {
    "serve": "bun src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@aoe/ir": "workspace:*",
    "@aoe/observability": "workspace:*",
    "@aoe/query-engine": "workspace:*",
    "@aoe/runtime": "workspace:*",
    "@aoe/sdk": "workspace:*"
  },
  "devDependencies": {
    "@aoe/model-schema": "workspace:*"
  }
}
