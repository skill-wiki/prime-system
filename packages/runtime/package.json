{
  "name": "@prime-lang/runtime",
  "version": "0.1.0",
  "description": "Runtime for loading, executing, and evaluating compiled Primes",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prime-lang/types": "workspace:*",
    "yaml": "^2.8.3",
    "zod": "^4.0.0"
  },
  "license": "Apache-2.0"
}
