{
  "name": "@skill-wiki/runtime",
  "version": "0.2.0",
  "description": "Immutable Corpus snapshot activation, Unit metadata reads, and projection resolution",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@skill-wiki/corpus-schema": "workspace:*",
    "@skill-wiki/types": "workspace:*",
    "yaml": "^2.8.3",
    "zod": "^4.0.0"
  },
  "license": "Apache-2.0"
}
