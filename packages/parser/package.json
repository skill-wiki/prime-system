{
  "name": "@aoe/parser",
  "version": "0.2.0",
  "description": "Parser for .prime source files — converts source text to AST",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@aoe/types": "workspace:*"
  },
  "license": "Apache-2.0"
}
