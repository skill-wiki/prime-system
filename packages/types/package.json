{
  "name": "@skill-wiki/types",
  "version": "0.2.0",
  "description": "Compatibility source and AST types for the Kernary engine",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test --pass-with-no-tests",
    "lint": "tsc --noEmit"
  },
  "license": "Apache-2.0"
}
