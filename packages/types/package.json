{
  "name": "@aoe/types",
  "version": "0.2.0",
  "description": "Domain-neutral source and AST types for the AOE engine",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test --pass-with-no-tests",
    "lint": "tsc --noEmit"
  },
  "license": "Apache-2.0"
}
